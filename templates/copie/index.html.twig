{% extends 'base.html.twig' %}

{% block title %}Hello CopieController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Users/fabien.capoen/Documents/projet/forma_quiz/src/Controller/CopieController.php'|file_link(0) }}">src/Controller/CopieController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/fabien.capoen/Documents/projet/forma_quiz/templates/copie/QuizClos.twig'|file_link(0) }}">templates/copie/index.html.twig</a></code></li>
    </ul>

    {% for question in quiz.questions %}
        <p class="question-description">{{ question.question }} ( / {{ question.noteMax }} ) </p>
        {%  if (question.questionType.id==1) %}
            <a href="{{ path('app_reponse_QCM_create', {'id': question.id}) }}">
                <button class="create-comment-button button">Répondre</button>
            </a>
        {% else %}
            <a href="{{ path('app_reponse_QCR_create', {'id': question.id}) }}">
                <button class="create-comment-button button">Répondre</button>
            </a>
        {%  endif %}
    {% endfor %}
</div>
{% endblock %}
